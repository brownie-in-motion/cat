{
  "name": "raas",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "@fastify/static": "^6.5.0",
    "@fastify/websocket": "^7.1.1",
    "dotenv": "^16.0.3",
    "fastify": "^4.9.2"
  },
  "devDependencies": {
    "concurrently": "^7.5.0",
    "esbuild": "^0.15.13",
    "nodemon": "^2.0.20",
    "xterm": "^5.0.0",
    "xterm-addon-fit": "^0.6.0"
  },
  "scripts": {
    "build": "esbuild client/app.js --bundle --outdir=dist --minify",
    "start-server": "nodemon -w server server",
    "start-client": "esbuild client/app.js --bundle --outdir=dist --watch --sourcemap",
    "start": "concurrently npm:start-*"
  },
  "type": "module"
}
